name: Build Redmi Note 11 (spes) Sitanshu Kernel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Kernel Source
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
        submodules: 'recursive'

    - name: Set up Build Environment
      run: |
        sudo apt-get update || true
        sudo apt-get install -y --no-install-recommends \
          build-essential bc libncurses5-dev libssl-dev bison flex libelf-dev git python3 gcc g++ wget tar ccache lld clang gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi device-tree-compiler zip unzip

    - name: Cache Proton Clang Toolchain
      uses: actions/cache@v4
      with:
        path: ~/clang
        key: proton-clang-spes-v12

    - name: Setup Proton Clang Toolchain
      run: |
        if [ ! -f "$HOME/clang/bin/clang" ]; then
          rm -rf ~/clang
          mkdir -p ~/clang
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git ~/clang
        fi

    - name: Fix Driver Kconfig and Inject Serial Lock
      run: |
        # Auto-create all missing Kconfig files across wireless drivers
        find drivers/net/wireless/ -type d -exec touch {}/Kconfig \; 2>/dev/null || true

        # Inject Hardware Serial Lock into arch/arm64/kernel/setup.c
        if ! grep -q "cd7276a2" arch/arm64/kernel/setup.c; then
          echo "" >> arch/arm64/kernel/setup.c
          echo "/* Sitanshu Hardware Serial Lock */" >> arch/arm64/kernel/setup.c
          echo "static int __init sitanshu_check_serial(void) {" >> arch/arm64/kernel/setup.c
          echo "    if (!strstr(boot_command_line, \"cd7276a2\")) {" >> arch/arm64/kernel/setup.c
          echo "        panic(\"Unauthorized Hardware! Serial mismatch.\");" >> arch/arm64/kernel/setup.c
          echo "    }" >> arch/arm64/kernel/setup.c
          echo "    return 0;" >> arch/arm64/kernel/setup.c
          echo "}" >> arch/arm64/kernel/setup.c
          echo "late_initcall(sitanshu_check_serial);" >> arch/arm64/kernel/setup.c
        fi

    - name: Build Kernel Image
      run: |
        export PATH="$HOME/clang/bin:$PATH"
        export USE_CCACHE=1
        export CCACHE_DIR="$HOME/.ccache"
        ccache -M 10G

        mkdir -p out
        make -j$(nproc --all) O=out ARCH=arm64 stock_defconfig

        scripts/config --file out/.config --set-str CONFIG_LOCALVERSION "-Sitanshu"
        scripts/config --file out/.config --disable CONFIG_IKHEADERS || true
        scripts/config --file out/.config --disable CONFIG_DEBUG_INFO_BTF || true
        scripts/config --file out/.config --disable CONFIG_DEBUG_INFO || true

        make -j$(nproc --all) O=out \
          ARCH=arm64 \
          CC="ccache clang" \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
          LLVM_IAS=0 \
          KCFLAGS="-w -Wno-error" \
          KCPPFLAGS="-w" \
          Image.gz

    - name: Upload Kernel Artifact
      uses: actions/upload-artifact@v4
      with:
        name: RedmiNote11-Sitanshu-Locked-Kernel
        path: out/arch/arm64/boot/Image.gz
        
